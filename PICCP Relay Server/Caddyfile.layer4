{
    email {$ACME_EMAIL}
}

{$RELAY_DOMAIN}:80 {
    redir https://{$RELAY_DOMAIN}{uri} 308
}

{$RELAY_DOMAIN}:443 {
    @relay path /relay
    reverse_proxy @relay {$RELAY_HTTP_UPSTREAM}
    respond "Noctyra Relay is online. Use HTTPS or WSS on /relay." 200
}
