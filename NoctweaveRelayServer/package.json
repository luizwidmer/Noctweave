{
  "name": "@noctweave/relay-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Source-built desktop operator launcher for NoctweaveRelayServer.",
  "type": "module",
  "scripts": {
    "desktop:dev": "electrobun dev",
    "desktop:watch": "electrobun dev --watch",
    "desktop:build": "electrobun build --env=stable",
    "desktop:test": "bun test desktop/test",
    "typecheck:desktop": "tsc --noEmit"
  },
  "license": "AGPL-3.0-or-later",
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/three": "^0.185.1",
    "electrobun": "^1.18.1",
    "typescript": "^7.0.2"
  }
}
